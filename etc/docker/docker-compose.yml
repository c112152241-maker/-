services:
  abpsolution2-web:
    image: abpsolution2:latest
    container_name: abpsolution2
    build:
      context: ../../
      dockerfile: AbpSolution2/Dockerfile.local
    environment:
      - ASPNETCORE_URLS=https://+:8081;http://+:8080;
      - Kestrel__Certificates__Default__Path=/app/certs/localhost.pfx
      - Kestrel__Certificates__Default__Password=91f91912-5ab0-49df-8166-23377efaf3cc
      - App__HealthCheckUrl=http://abpsolution2:8080/health-status
      - ConnectionStrings__Default=DataSource=/data/AbpSolution2.db
    ports:
      - "44353:8081"
    depends_on:
      sqlite:
        condition: service_healthy
    restart: on-failure  
    volumes:
      - sqlite_data:/data
      - ./certs:/app/certs
    networks:
      - abp-network  
  
  db-migrator:
    image: abpsolution2:latest
    container_name: db-migrator
    build:
      context: ../../
      dockerfile: AbpSolution2/Dockerfile.local
    environment:
      - ConnectionStrings__Default=DataSource=/data/AbpSolution2.db
    command:
      - --migrate-database
    depends_on:
      sqlite:
        condition: service_healthy
    volumes:
      - sqlite_data:/data
    networks:
      - abp-network
  sqlite:
    container_name: sqlite-container
    image: alpine:latest
    command: sh -c "apk add sqlite && touch /data/AbpSolution2.db && sqlite3 /data/AbpSolution2.db '.databases' && tail -f /dev/null"
    environment:
      - TZ=UTC
    volumes:
      - sqlite_data:/data
    networks:
      - abp-network
    healthcheck:
      test: ["CMD-SHELL", "if [ -f /data/AbpSolution2.db ]; then exit 0; else exit 1; fi"]
      interval: 10s
      timeout: 3s
      retries: 10
volumes:
  sqlite_data:
    name: abpsolution2_sqlite_data
networks:
  abp-network:
    name: abpsolution2-network
    driver: bridge